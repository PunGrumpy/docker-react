language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t pungrumpy/docker-react -f Dockerfile.dev .

script:
  # - docker run pungrumpy/docker-react npm run test -- --coverage
  - docker run -e CI=true pungrumpy/docker-react npm run test
